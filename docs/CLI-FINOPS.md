# Claude FinOps is now AUM

The terminal console is now **AUM - Azure Usage Management**.
Use the `aum` command and the [AUM guide](AUM.md).

`claude-finops` remains a deprecated command alias for one release. Existing
configuration files and the internal Python package paths remain compatible.
